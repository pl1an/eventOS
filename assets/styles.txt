hey! this file contains colors and styles in general for Event OS (and data formats apparently)

maindata info format = {
    dates_currentyear:number[][][]
    month_names:string[]
    monthevents: day info[]
}

day info format = {
    events: event info[]
    placeholderevents:[event info, event info, event info]
}

event info format = {
    tag:{
        color:string
        name:string
    },
    eventname:string,
    time:[number, number],
    date:[number, number],
    repeattime:number,
    datailedinfo:string[]
}